[deploy]
preDeployCommand = ["cd /app/packages/common && uv run alembic upgrade head"]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10